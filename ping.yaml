---
- hosts: "{{ target_hosts }}"
  tasks:
    - name: Ping server
      ansible.builtin.ping:
      register: testt
      
    - name: Display the result
      ansible.builtin.debug:
        var: testt
